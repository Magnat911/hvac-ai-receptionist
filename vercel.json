{
  "buildCommand": "cp requirements-vercel.txt requirements.txt && pip install -r requirements.txt",
  "outputDirectory": ".",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ]
}
